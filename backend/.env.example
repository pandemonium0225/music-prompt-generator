# =============================================================================
# Backend 環境變數設定
# =============================================================================
# 複製此檔案為 .env 並修改設定值
# cp .env.example .env
# =============================================================================

# 伺服器端口 (預設: 8000)
PORT=8000

# 執行環境: development | production
ENVIRONMENT=development

# 日誌等級: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# CORS 允許的來源 (逗號分隔，生產環境請設定具體網域)
# 開發環境預設為 * (允許所有)
# 生產環境範例: https://your-frontend.run.app,https://yourdomain.com
ALLOWED_ORIGINS=*

# =============================================================================
# CLAP 模型設定 (v2.0+)
# =============================================================================

# 是否啟用 CLAP 語義分析 (true | false)
# 設為 false 可降低記憶體需求，僅使用 Librosa 數值分析
CLAP_ENABLED=true

# CLAP 模型名稱 (HuggingFace 模型 ID)
# 預設使用 LAION 的音樂專用模型
CLAP_MODEL=laion/larger_clap_music

# CLAP 返回的標籤數量 (預設: 8)
CLAP_TOP_K=8

# CLAP 信心度閾值 (預設: 0.015)
# 當 CLAP 最高分低於此值時，自動忽略 CLAP 結果
# 對華語/非西方音樂建議設為 0.02 或更高
CLAP_CONFIDENCE_THRESHOLD=0.015

# =============================================================================
# LLM 設定 (v2.1+)
# =============================================================================

# 是否啟用 LLM 自然語言生成 (true | false)
# 設為 true 會使用 GPT 生成流暢的音樂描述
LLM_ENABLED=true

# OpenAI API Key (必填，如果 LLM_ENABLED=true)
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI 模型 (預設: gpt-4o-mini，較便宜且快速)
# 可選: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini
